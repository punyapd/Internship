class Block {
  constructor(x, y, width, height, isgoal, color, direction) {
    this.x = x;
    this.y = y;
    this.height = height;
    this.width = width;
    this.isgoal = isgoal;
    this.color = color;
    this.direction = direction;
  }
}
